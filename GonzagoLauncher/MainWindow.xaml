<Window x:Class="GonzagoLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GonzagoLauncher"
        mc:Ignorable="d"
        Title="Gonzago Launcher by RedGrox"
        WindowStartupLocation="CenterScreen"
        ThemeMode="System"
        MinWidth="750" MinHeight="500" Height="600" Width="800">

    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="80"
                   Margin="30"
                   FontFamily="./Resources/Fonts/#SporedomRUS"
                   Text="GonzagoGL" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock>Mode</TextBlock>
            <ComboBox SelectedIndex="{Binding SelectedModeIndex}" Margin="0,3" ItemsSource="{Binding Modes}" />
            <TextBlock>
                Command line arguments
            </TextBlock>
            <TextBox Text="{Binding CommandLineArguments}" Margin="0,3">
                <TextBox.ToolTip>
                    GonzagoGL command line list:
                    -exception
                    -noautopause
                    -loadgame
                    -flocktestmode
                    -spacemode
                    -civmode
                    -citymode
                    -tribemode
                    -noedgescroll
                    -fullscreen
                    -twomonitors
                    -nosound
                    -usecivtestcamera
                </TextBox.ToolTip>
            </TextBox>

            <Button FontSize="30"
                    Margin="0, 50, 0, 0"
                    Width="300"
                    Cursor="Hand"
                    Style="{DynamicResource AccentButtonStyle}"
                    IsEnabled="{Binding PlayButtonIsEnabled}"
                    Command="{Binding PlayButtonCommand}"
                    Content="Play" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{Binding Status}" HorizontalAlignment="Center" />
            <ProgressBar IsIndeterminate="{Binding IsIndeterminate}"
                         Value="{Binding ProgressValue}"
                         Maximum="{Binding MaxProgressValue}" />
        </StackPanel>
    </Grid>
</Window>
